# Media Tools

Place tested versions of:

```text
yt-dlp.exe
ffmpeg.exe
ffprobe.exe
```

in `tools/bin/` for local development.

For production distribution, pin versions and document their licenses and redistribution terms. The repository does not include third-party binaries by default.
